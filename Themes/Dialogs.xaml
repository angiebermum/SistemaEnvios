<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="DialogWindowStyle" TargetType="Window" BasedOn="{StaticResource AppWindowStyle}">
        <Setter Property="Icon" Value="pack://application:,,,/Images/icon.ico" />
        <Setter Property="ShowInTaskbar" Value="False" />
        <Setter Property="Background" Value="{StaticResource CanvasBrush}" />
    </Style>
    <Style x:Key="InfoPanelStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource PrimaryLightBrush}" />
        <Setter Property="BorderBrush" Value="#C5DDEE" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="7" />
        <Setter Property="Padding" Value="12" />
    </Style>
    <Style x:Key="WarningPanelStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource WarningBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="#E8CF88" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="7" />
        <Setter Property="Padding" Value="12" />
    </Style>
</ResourceDictionary>
