<Window x:Class="ECS.CommissionsMailer.Views.ExpirationsAssistantEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Asistente de Vencimientos" Width="520" Height="360" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" Style="{StaticResource DialogWindowStyle}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="Asistente de Vencimientos" Style="{StaticResource PageTitleTextStyle}" />
        <TextBlock Grid.Row="1" Text="Este asistente pertenece únicamente al módulo de Vencimientos."
                   Style="{StaticResource PageSubtitleTextStyle}" Margin="0,4,0,18" />
        <StackPanel Grid.Row="2">
            <TextBlock Text="Nombre" Style="{StaticResource CaptionTextStyle}" />
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" MaxLength="160" />
        </StackPanel>
        <StackPanel Grid.Row="3" Margin="0,14,0,0">
            <TextBlock Text="Correo" Style="{StaticResource CaptionTextStyle}" />
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" MaxLength="254" />
        </StackPanel>
        <ToggleButton Grid.Row="4" Content="Activo" IsChecked="{Binding IsActive, Mode=TwoWay}"
                      Style="{StaticResource SwitchToggleButtonStyle}"
                      HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,16,0,0" />
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancelar" IsCancel="True" Style="{StaticResource CompactButtonStyle}" />
            <Button Content="Guardar" Click="Save_Click" IsDefault="True"
                    Style="{StaticResource PrimaryButtonStyle}" Margin="0" />
        </StackPanel>
    </Grid>
</Window>
