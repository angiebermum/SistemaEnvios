<Window x:Class="ECS.CommissionsMailer.Views.GenerationPeriodWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Generar archivos" Width="680" Height="510" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" Style="{StaticResource DialogWindowStyle}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="Generar detalles de pago" Style="{StaticResource PageTitleTextStyle}" />
            <TextBlock Text="La generación crea archivos; no abre Outlook ni envía correos."
                       Style="{StaticResource MutedTextStyle}" Margin="0,5,0,0" />
        </StackPanel>
        <StackPanel Grid.Row="1" Margin="0,20,0,0">
            <TextBlock Text="Nombre de la quincena o periodo" Style="{StaticResource FieldLabelTextStyle}" />
            <TextBox x:Name="PeriodTextBox" TextChanged="PeriodTextBox_TextChanged" />
            <TextBlock Text="Ejemplo: IIQ julio 2026" Style="{StaticResource CaptionTextStyle}" Margin="2,4,0,0" />
        </StackPanel>
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="14" Margin="0,18,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Text="Carpeta" FontWeight="SemiBold" />
                <TextBlock Grid.Column="1" x:Name="FolderPreviewTextBlock" TextWrapping="Wrap" />
                <TextBlock Grid.Row="1" Text="Archivo de ejemplo" FontWeight="SemiBold" Margin="0,8,0,0" />
                <TextBlock Grid.Row="1" Grid.Column="1" x:Name="FilePreviewTextBlock"
                           TextWrapping="Wrap" Margin="0,8,0,0" />
                <TextBlock Grid.Row="2" Text="Pestañas / archivos" FontWeight="SemiBold" Margin="0,8,0,0" />
                <TextBlock Grid.Row="2" Grid.Column="1" x:Name="WorksheetCountTextBlock" Margin="0,8,0,0" />
                <TextBlock Grid.Row="3" Text="Corredores" FontWeight="SemiBold" Margin="0,8,0,0" />
                <TextBlock Grid.Row="3" Grid.Column="1" x:Name="BrokerCountTextBlock" Margin="0,8,0,0" />
                <TextBlock Grid.Row="4" Text="Estado de carpeta" FontWeight="SemiBold" Margin="0,8,0,0" />
                <TextBlock Grid.Row="4" Grid.Column="1" x:Name="FolderStateTextBlock" Margin="0,8,0,0" />
            </Grid>
        </Border>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancelar" IsCancel="True" />
            <Button Content="Generar" Click="Generate_Click" IsDefault="True"
                    Style="{StaticResource PrimaryButtonStyle}" Margin="0" />
        </StackPanel>
    </Grid>
</Window>
